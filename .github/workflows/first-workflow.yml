name: sanity-check

on:
  push:
    branches: [ main ]


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow triggered now real checkk"

      - name: Read file
        run: cat README.md
